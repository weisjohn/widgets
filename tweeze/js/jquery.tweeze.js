/*


*/
(function($) {
	
	$.fn.tweeze = function(options) {  
		  
		var defaults = {
			
			username: 'weisjohn',
			
		};
		var options = $.extend(defaults, options);
		
		// TODO: write some functions to grab results from twitter
		
	    return this.each(function() {  
	  
	    	
	    	
	    });  

	};  
	
})(jQuery);